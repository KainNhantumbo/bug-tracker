export type ThemeType = {
	primary: string;
	secondary: string;
	accent: string;
	font: string;
	neutral: string;
	background: string;
	foreground: string;
	alternative_a: string;
	alternative_b: string;
};
